# Configuracion de pytest. Vive en la raiz del repo a proposito: si esta en
# tests/, `pytest` a secas desde la raiz no la encuentra y avisa que el
# marcador 'lento' es desconocido.
[pytest]
testpaths = tests
markers =
    lento: abre los Excel de data/raw/ (tarda decenas de segundos)
