% =============================================================================
%  avance_socioformador.tex - presentacion breve de avance (issue #64)
%  VERSION SIMPLE: lenguaje llano, sin jerga estadistica. Ver tambien
%  avance_socioformador_tecnico.tex (misma estructura, cifras identicas,
%  redaccion tecnica) para comparar las dos opciones.
%
%  NO es la presentacion final de entrega: es un medio para mostrarle al
%  socioformador el avance tras el cambio de objetivo y recibir
%  retroalimentacion. Compila independiente de reports/ (misma carpeta de
%  figuras, via \graphicspath, para no duplicar archivos).
%
%  Estado (2026-09-03): las 3 diapositivas de contenido estan completas.
%  Cifras tomadas de reports/secciones/objetivo_tecnicas.tex,
%  reports/secciones/pca.tex y reports/secciones/regresion_logistica.tex
%  (que ya incluye logistica, discriminante y su comparacion, traidas a
%  esta rama desde la PR #63, regresion-logistica-analisis-discriminante).
%
%  Compilar desde esta carpeta:   latexmk
% =============================================================================

\documentclass{beamer}

\usetheme{Madrid}
\usecolortheme{default}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
% Igual que reports/main.tex: es-nodecimaldot evita que babel convierta
% los puntos decimales en comas dentro de modo matematico.
\def\spanishoptions{es-tabla,es-nodecimaldot,es-noquoting}
\usepackage[spanish]{babel}
\usepackage{enumitem}

% Reutiliza las figuras ya generadas para el reporte; no se duplican aqui.
\graphicspath{{../reports/figuras/}}

\setbeamertemplate{navigation symbols}{}
\setbeamertemplate{caption}[numbered]

\title[Avance: excedencias de ozono]{Clasificación de excedencias de ozono en el AMM}
\subtitle{Presentación de avance para el socioformador --- versión simple}
\author{Equipo 102.07}
\institute[MA2003B --- 102.07]{MA2003B --- Aplicación de Métodos Multivariados en Ciencia de Datos}
\date{\today}

\begin{document}

\begin{frame}
  \titlepage
  \begin{center}
    \small Avance del proyecto tras el cambio de objetivo; no es la presentación final de entrega.
  \end{center}
\end{frame}

% -----------------------------------------------------------------------
% Diapositiva 1: objetivo + PCA
% Fuente: reports/secciones/objetivo_tecnicas.tex y reports/secciones/pca.tex
% -----------------------------------------------------------------------
\begin{frame}{¿Qué buscamos y cómo simplificamos los datos?}
  \footnotesize

  \textbf{El objetivo.} Identificar, día por día y por estación, cuándo
  el aire del Área Metropolitana de Monterrey superó el límite seguro
  de ozono que marca la norma oficial (\textbf{51 ppb},
  NOM-020-SSA1-2021), según los contaminantes y el clima de ese mismo
  día. El modelo \textbf{explica} qué condiciones acompañan esos
  episodios; no \textbf{predice} los siguientes.
  \vspace{4pt}

  \begin{columns}[T]
    \begin{column}{0.56\textwidth}
      \textbf{¿Por qué componentes principales?} Muchas de las 15
      variables medidas se mueven juntas: \texttt{NO} y \texttt{NOX},
      dos contaminantes clave, casi se comportan como uno solo
      (correlación de 0.94). Para no repetir información, se resumen
      en 3 grandes ejes:
      \begin{itemize}[itemsep=1pt,topsep=2pt,parsep=0pt,leftmargin=*]
        \item \textit{Carga primaria de combustión}: contaminación
          por quema de combustibles
        \item \textit{Forzamiento fotoquímico}: qué tan soleado y
          seco está el día
        \item \textit{Estancamiento atmosférico}: qué tanto se
          acumula el aire contaminado
      \end{itemize}
      Juntos conservan \textbf{55.9\,\%} de esa información, y
      alimentan los modelos que siguen.
    \end{column}
    \begin{column}{0.44\textwidth}
      \centering
      \includegraphics[width=0.92\linewidth]{pca_scree.pdf}

      \footnotesize Varianza explicada por componente.
    \end{column}
  \end{columns}
\end{frame}

% -----------------------------------------------------------------------
% Diapositiva 2: regresion logistica
% Fuente: reports/secciones/regresion_logistica.tex, subseccion
% "Regresion Logistica".
% -----------------------------------------------------------------------
\begin{frame}{Regresión logística: primeros resultados}
  \footnotesize

  \textbf{Partición.} Entrenamiento con 2021--2024; validación con
  2025, fechas que el modelo no vio durante el ajuste.
  \vspace{4pt}

  \begin{columns}[T]
    \begin{column}{0.56\textwidth}
      \textbf{¿Qué tan bien clasifica?} En validación, el modelo
      distingue los días con y sin excedencia con un AUC de
      \textbf{0.893} --- muy por encima de la meta de 0.75. Detecta el
      \textbf{83.1\,\%} de las excedencias reales (sensibilidad) y
      descarta correctamente el \textbf{82.4\,\%} de los días sin
      excedencia (especificidad).

      No se usa la exactitud como criterio: 41.5\,\% de los
      días-estación de validación excede el umbral, así que un modelo
      que solo adivinara la clase más común ya luciría bien sin
      detectar nada real.

      De los 3 ejes del ACP, el que más empuja hacia la excedencia es
      el \textit{forzamiento fotoquímico}, seguido de la
      \textit{carga primaria de combustión}; el
      \textit{estancamiento atmosférico} influye menos.
    \end{column}
    \begin{column}{0.44\textwidth}
      \centering
      \includegraphics[width=0.92\linewidth]{momios_componentes.pdf}

      \footnotesize Qué tanto empuja cada eje hacia la excedencia.
    \end{column}
  \end{columns}
\end{frame}

% -----------------------------------------------------------------------
% Diapositiva 3: discriminante + comparacion + cierre
% Fuente: reports/secciones/regresion_logistica.tex, subsecciones
% "Analisis discriminante lineal", "Comparacion de los dos modelos" y
% "Limitaciones".
% -----------------------------------------------------------------------
\begin{frame}{¿El discriminante da lo mismo? Comparación y cierre}
  \footnotesize

  \textbf{El discriminante lineal} es la otra técnica que probamos.
  Parte de un supuesto más fuerte que la logística: que los datos
  siguen una distribución normal y que se dispersan igual en ambos
  grupos. Lo segundo no se cumple del todo --- los días de excedencia
  varían menos que los de no excedencia ---, pero en la práctica no
  importó.
  \vspace{4pt}

  \begin{columns}[T]
    \begin{column}{0.56\textwidth}
      También alcanza un AUC de \textbf{0.893} en validación: detecta
      el \textbf{84.5\,\%} de las excedencias reales y acierta el
      \textbf{81.1\,\%} de los días sin excedencia --- casi idéntico a
      la logística.

      De hecho, los dos modelos coinciden en el \textbf{98.1\,\%} de
      sus decisiones, y una prueba estadística no encuentra diferencia
      real entre sus curvas ROC.

      \textbf{Cierre.} Aunque el discriminante rompe uno de sus
      supuestos, llega prácticamente al mismo resultado que el modelo
      que no lo necesita. Queda pendiente: los modelos explican qué
      acompaña una excedencia el mismo día, no predicen el siguiente;
      y como varias estaciones comparten el mismo clima, los días no
      son del todo independientes entre sí.
    \end{column}
    \begin{column}{0.44\textwidth}
      \centering
      \includegraphics[width=0.92\linewidth]{roc_modelos.pdf}

      \footnotesize Las dos curvas se superponen casi por completo.
    \end{column}
  \end{columns}
\end{frame}

\end{document}
